service: serviceless-hello-world

provider:
    name: aws
    runtime: nodejs12.x

functions:
    test:
        handler: ./dist/index.handler
        name: serverless-hello-world
        events: 
            - hhtpApi:
                method: GET
                path: /
